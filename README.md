# assistant-walkthrough

A self-contained, interactive walkthrough for the family-assistant (Claude Code + Obsidian vault).
Open `index.html` in any browser, or view the published page via GitHub Pages.

Single file, no dependencies. Source of truth lives in Reut's vault
(`03/Projects/family-assistant/assets/walkthrough.html`); this repo is the published copy —
to update, re-copy that file over `index.html` and push.
